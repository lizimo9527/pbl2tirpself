// 本文件为自动构建生成
import {
  CreateElement as CreateElementOrigin,
  Uni as UniOrigin
} from './interface'

declare global {
  type CreateElement = CreateElementOrigin
  interface Uni extends UniOrigin { }
}
