import type { APP_PLATFORM } from './manifest/utils';
import type { CompileResult } from './index';
export type EncryptArtifacts = {
    env: {
        compilerVersion: string;
    } & Record<string, any>;
    apis: string[];
    components: string[];
    scopedSlots: string[];
    customElements: {
        name: string;
        class: string;
    }[];
    declaration: string;
};
export declare function isEncrypt(pluginDir: string): boolean;
export declare function isUTSModules(pluginDir: string): boolean;
export declare function compileEncrypt(pluginDir: string, isX?: boolean): Promise<CompileResult>;
export declare function resolveJsCodeCacheFilename(platform: APP_PLATFORM, cacheDir: string, pluginRelativeDir: string): string;
