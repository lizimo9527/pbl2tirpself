import type * as tsTypes from 'typescript';
import type { RollupError } from 'rollup';
import type { UniXCompiler, UniXCompilerOptions } from '../../lib/uni-x/dist/compiler';
export type { UniXCompiler } from '../../lib/uni-x/dist/compiler';
type TargetLanguage = `${UniXCompilerOptions['targetLanguage']}`;
export declare function createUniXCompiler(mode: UniXCompilerOptions['mode'], targetLanguage: TargetLanguage, options: {
    inputDir: string;
    outputDir: string;
    cacheDir: string;
    paths?: tsTypes.CompilerOptions['paths'];
    rootFiles?: string[];
    normalizeFileName: (str: string) => string;
    isPureSwift?: boolean;
    resolveWorkers: () => Record<string, string>;
}): UniXCompiler;
interface DiagnosticError {
    msg: string;
    file?: string;
    line?: number;
    column?: number;
    frame?: string;
}
export declare function createRollupError(error: DiagnosticError): RollupError;
declare module 'typescript' {
    interface Diagnostic {
        __throwError?: boolean;
    }
}
